{
  "name": "fashionhub",
  "version": "1.0.0",
  "description": "Clothing Store - React + Node.js",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.0",
    "multer": "^1.4.5-lts.1",
    "jsonwebtoken": "^9.0.0",
    "nodemailer": "^6.9.3",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}